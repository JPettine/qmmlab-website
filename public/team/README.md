# Team profiles

Add one image and one text file per team member using the same `FirstnameLastname` file stem.

Example:

- `FirstnameLastname.jpg`
- `FirstnameLastname.txt`

Text file format:

1. Position
2. Email address
3. Optional note

Images are cropped consistently on the Team page with CSS, so source photos can have different dimensions.
